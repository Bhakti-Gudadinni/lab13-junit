import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Test;

public class CalculatorTest {

    @Test
    void testAddition() {
        // Simple test to ensure Maven finds and runs JUnit 5
        assertEquals(4, 2 + 2);
    }
}
